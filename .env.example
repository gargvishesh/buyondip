# PostgreSQL — Railway auto-sets these when you add the Postgres plugin
# For local Docker testing, set these manually
PGHOST=localhost
PGPORT=5432
PGDATABASE=buyondip
PGUSER=postgres
PGPASSWORD=yourpassword

# PORT is auto-set by Railway; not needed locally (defaults to 8080)
# PORT=8080
